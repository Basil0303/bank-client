import { DierativeDirective } from './dierative.directive';

describe('DierativeDirective', () => {
  it('should create an instance', () => {
    const directive = new DierativeDirective();
    expect(directive).toBeTruthy();
  });
});
